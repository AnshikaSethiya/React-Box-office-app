import React from 'react'

const Title = ({title, subtitle}) => {
    return (
        <div>
            <h1>{title}</h1>
            <h5>{subtitle}</h5>
        </div>
    )
}

export default Title

